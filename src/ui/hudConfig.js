export const HERO_PORTRAIT_STATE_DURATIONS_MS = {
  attack: 750,
  hurt: 700,
  cast: 850,
  coin: 1100,
};

export const DEFAULT_HERO_PORTRAIT_DURATION_MS = 850;

export const RESOLUTION_TIMINGS_MS = {
  swap: 120,
  invalidOut: 100,
  invalidBack: 90,
  matchHighlight: 150,
  clear: 120,
  effectPopups: 200,
  fallBase: 90,
  fallPerRow: 55,
  fallPerRowHeavy: 40,
  fallMax: 260,
  spawn: 130,
  cascadePause: 120,
};

export const SELECTION_VFX = {
  strokeWidth: 3,
  strokeColor: 0xf6f7fb,
  idleStrokeWidth: 2,
  idleStrokeColor: 0x293242,
  scale: 1.05,
  glowAlpha: 0.22,
};

export const IDLE_HINT_DELAY_MS = 4000;
export const START_OVERLAY_MS = 900;

export const PHASE_BANNER = {
  castText: "CAST (optional)",
  swapText: "SWAP",
};
